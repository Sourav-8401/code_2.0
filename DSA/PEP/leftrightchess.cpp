#include <iostream>
#include <bits/stdc++.h>
#include <string>
using namespace std;

// 
bool match(vector<int>& tempW, vector<int>& freq){
    return true;
}
vector<int> find(int n, int side, vector<pair<int, int>> move){
    int fl=0, ft=0, fr=0, fb=0;
    int l=0,t=0,r=0,b=0;

    for(int i=0; i<n; i++){
        int tochl = side-move[i].second;
        int tochl = side-move[i].first;

    }
}
bool find(string initial, string t){
    int ilen = initial.size();
    int flen = t.size();
    if(ilen != flen){
        return false;
    }
    int cntl=0, cntr=0;
    vector<int> initalCnt(findfreq())
}
int main(){


}